# jd-image-web-ui

Server-deployed image generation web application. The only product form is a server running Web, Worker, and PostgreSQL; users sign in through an internal HTTP browser URL.

See the [Chinese README](README.md) and [server operations guide](deploy/server/README.md) for deployment, external PostgreSQL, backup, restore, maintenance lock, and storage reconciliation instructions.
