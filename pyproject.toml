[project]
name = "ilab-gpt-conjure"
version = "0.1.0"
description = "Server-only image generation Web application with PostgreSQL and Worker."
requires-python = ">=3.11"

license = "AGPL-3.0-only"
[project.scripts]
jd-image-ops = "codex_image.server.ops:main_entry"
